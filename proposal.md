# 项目申报书：moon-typst —— Typst 标记语言解析与 SVG/HTML 排版渲染引擎

## 一、基本信息
- **项目名称**：`moon-typst`（MoonBit Typst 标记语言解析、盒模型排版与矢量渲染引擎）
- **项目方向**：语言和开发工具 / 文档排版与可视化基础设施
- **参赛者 GitHub**：Lxxbv
- **仓库链接**：https://github.com/Lxxbv/moon-typst
- **是否为移植项目**：是（参考 Typst 官方排版理念与核心标记语法规范）
- **开源许可证**：Apache-2.0
- **原项目参考**：Typst (https://github.com/typst/typst, Apache-2.0)

## 二、项目简介与生态痛点
Typst 作为面向现代科研与工程领域的新一代开源排版系统，凭借简洁明了的标记语法与亚毫秒级的增量编译速度，正迅速成为 LaTeX 的现代替代方案。MoonBit 作为面向 AI 时代的新一代编程语言，具备极致小巧的 WASM 编译产物与极速执行效率，天然契合 Web 端、IDE 插件与轻量终端的即时文档渲染场景。

然而，经过对 mooncakes.io 全站 2493 个模块与 GitHub 社区的全面查重，当前 MoonBit 生态在排版与渲染基础设施领域存在显著空白（仅有离散的数学公式字符串解析工具，无任何通用排版引擎）。本项目旨在用纯 MoonBit 语言实现一套零外部 C-FFI 依赖的轻量级 Typst 核心解析、盒模型几何排版与 SVG/HTML 矢量渲染引擎，直接解决 MoonBit 应用在无重型渲染守护进程环境下的高质量富文档输出痛点。

## 三、通用性与 3 个完整预期使用场景
1. **IDE 与浏览器轻量即时排版预览（WASM 极速端）**：
   在 VS Code 插件或 Web 端代码编辑器中直接运行编译为 WebAssembly 的 `moon-typst`。用户在左侧输入 Typst 标记文本，右侧在数毫秒内无缝渲染出高质量 SVG 矢量排版预览，无需在本地安装数十兆的后端守护进程。
2. **轻量技术报告与自动化账单/报表渲染管道**：
   在 MoonBit 微服务、边缘服务或自动化批处理脚本中，动态注入业务数据拼接生成 Typst 模板，利用本库批量编译导出带有公式、统计表格、多级标题的矢量图与自包含 HTML 报表。
3. **知识库与静态文档站点的高级排版引擎**：
   作为静态站点生成器（如 MoonBit Rabbita 架构）或富文本内容管理系统的高阶排版后端，替代传统表现力有限的 Markdown，提供公式美化、精准段落对齐与可配置样式环境。

## 四、核心功能范围与交付边界
### 核心交付功能：
1. **Typst 核心标记词法与语法解析（Lexer & Parser）**：
   - 支持多级标题（`= `, `== `, `=== `）；
   - 支持行内格式修饰（`*加粗*`、`_斜体_`、`` `行内代码` ``、超链接）；
   - 支持多行代码块（```` ```lang ... ``` ````）；
   - 支持无序列表（`- item`）与有序编号列表（`+ item`）；
   - 支持行内与块级数学公式（如 `$E = m c^2$`, `$\frac{a}{b}$`）；
   - 支持结构化表格（Table）与水平分割线。
2. **文档对象模型（Document AST）与层级样式上下文**：
   构建类型安全的不可变 AST 树，支持页面边距、字体大小、行高、段落间距与对齐属性的层级样式解析。
3. **流式盒模型排版引擎（Layout Engine）**：
   实现字符度量盒（Box Model）、文本流折行（Word Wrap）、分段间距与数学符号上下标排版几何计算。
4. **多后端矢量与二进制渲染器**：
   - `render_pdf`：**100% 纯 MoonBit 实现的 Adobe PDF 1.4 二进制生成器**，支持间接对象、xref 交叉引用表、Type1 字体字典与绘制流；
   - `render_svg`：输出标准自包含 SVG 矢量图形（文本轮廓、几何路径、公式结构）；
   - `render_html`：输出符合语义化规范的 HTML5 结构并内嵌响应式样式。
5. **CLI 工具、高亮诊断系统与端到端可复现样例**：
   提供 `typst-render` 命令行工具，支持 `.typ` 到 `.pdf` / `.svg` / `.html` 的转换；内置 Rustc 风格彩色终端诊断报告器（`DiagnosticReporter`）；提供学术论文 Abstract、多列高等数学论文、幻灯片演示文稿、个人简历、技术规范说明等真实可复现用例。

### 明确不做（Non-Goals）：
- 不实现 Typst 完整的图灵完备脚本虚拟机（如动态闭包、动态作用域宏与图灵完备反射系统），严格限定在排版标记语言与渲染核心。
- 暂不包含复杂的本地 C 字体字形轮廓解析器（排版引擎采用通用度量盒模型与系统矢量字型方案）。

## 五、实现路径与技术理解
1. **纯函数式 AST 与模式匹配架构**：
   利用 MoonBit 强大的 Pattern Matching 与 ADT（代数数据类型）构建类型安全的语法树，规避指针悬挂与内存冗余，确保纯内存运行无崩溃。
2. **多阶段管线解耦**：
   清晰解耦 `Source -> Token Stream -> AST -> Layout Boxes -> SVG/HTML Output`，每个阶段均具备独立的差分测试与断言。
3. **交付物承诺**：
   - 托管于 GitHub 公开仓库，不少于 10 个规范递进 commit；
   - 完备的单元测试覆盖核心解析与渲染路径；
   - 自动化跨平台 CI（GitHub Actions 覆盖 check / test / build）；
   - 发布至 mooncakes.io（包名：`Lxxbv/moon_typst`）。
